CLI for [OsuFileIO](https://github.com/Xarib/OsuFileIO)

Command line args:
```
OsuFileIO_CLI.exe {input} {output}
```

Using `checksum` for output will use the map's MD5 hash as the output file name.